# Ro Bot

```bash
git clone https://github.com/Lrcezimbra/skypebot
cd skypebot
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp contrib/env-sample .env
# Add your credentials in .env
python skypebot
```
